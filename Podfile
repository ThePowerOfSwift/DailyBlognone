# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'DailyBlognone' do
  use_frameworks!
  pod 'ViewElements'
  pod 'AEXML'
  pod 'Google-Mobile-Ads-SDK'
  target 'DailyBlognoneTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
